<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind?"
                class="w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md placeholder-gray-400 focus:border-blue-500"
                name="content"><%= post.content %></textarea>

            <!-- Delete Button -->
            <div class="flex gap-3">
                <a href="/delete/<%= post._id %>"
                    class="px-3 py-2 w-40 bg-red-600 text-white block rounded-md mt-2 text-sm text-center">
                    Delete Post
                </a>

                <!-- Update Button -->
                <input class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-md mt-2 text-sm" type="submit"
                    value="Update Post">
            </div>
        </form>
    </div>
</body>


</html>